type: postgres
driver: org.postgresql.Driver
url: jdbc:postgresql://{{endpoint}}:5432/postgres?sslmode=require&ApplicationName=featurebench&reWriteBatchedInserts=true
username: {{username}}
password: {{password}}
batchsize: 128
isolation: TRANSACTION_REPEATABLE_READ
loaderThreads: 4
terminals: 1
collect_pg_stat_statements: true
yaml_version: v1.0
works:
    work:
        time_secs: 180
        active_terminals: 1
        rate: unlimited
        warmup: 60
microbenchmark:
    class: com.oltpbenchmark.benchmarks.featurebench.customworkload.YBDefaultMicroBenchmark
    properties:
        setAutoCommit: true
        # Row-shape scan experiment: 100 TEXT columns, ~10 chars each (~1024 B row), 10000000 rows. Same column layout as seq twin (lookup_key unindexed, unused here); idx workload: WHERE id = ? (PK). Scan mode: idx.
        create:
            - DROP TABLE IF EXISTS THRPT_between_range_1000;
            - ALTER DATABASE postgres SET enable_seqscan=off;
            - ALTER DATABASE postgres SET enable_bitmapscan=off;
            - CREATE TABLE THRPT_between_range_1000 (id INT, c1 int, c2 int, payload text, PRIMARY KEY(id));

        cleanup:
            - DROP TABLE IF EXISTS THRPT_between_range_1000;
            - ALTER DATABASE postgres SET enable_seqscan TO DEFAULT;
            - ALTER DATABASE postgres SET enable_bitmapscan TO DEFAULT;

        loadRules:
            - table: THRPT_between_range_1000
              rows: 1000000
              columns:
                  - name: id
                    util: RandomUniqueIntGen
                    params: [1, 1000000]
                  - name: c1
                    util: RandomUniqueIntGen
                    params: [1, 1000000]
                  - name: c2
                    util: RandomUniqueIntGen
                    params: [1, 1000000]
                  - name: payload
                    util: RandomAString
                    params: [1024, 1024]
        executeRules:
            - workload: THRPT_next_pk_btwn_1000
              run:
                  - name: THRPT_next_pk_btwn_1000
                    weight: 100
                    queries:
                        - query: SELECT * FROM THRPT_between_range_1000 WHERE id between ? and ?;
                          bindings:
                              - util: RandomNumber
                                params: [1, 999000]
                                referenceName: id1
                              - util: ExpressionEval
                                params:
                                    - "id1 + 1000"