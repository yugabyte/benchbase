# Vector Delete - 768D, single row delete with HNSW index
# Following VectorDBBench naming conventions
# Configuration: dimension=768, operation=delete, deletetype=single, indexed=true

type: POSTGRES
driver: org.postgresql.Driver
url: jdbc:postgresql://{{endpoint}}:5433/yugabyte?sslmode=require&ApplicationName=featurebench&reWriteBatchedInserts=true
username: {{username}}
password: {{password}}
batchsize: 128
isolation: TRANSACTION_REPEATABLE_READ
loaderthreads: 4
terminals: 1
collect_pg_stat_statements: true
use_dist_in_explain: true
yaml_version: v1.0
works:
    work:
        time_secs: 120
        active_terminals: 1
        rate: unlimited
        warmup: 30
microbenchmark:
    class: com.oltpbenchmark.benchmarks.featurebench.customworkload.YBDefaultMicroBenchmark
    properties:
        setAutoCommit: false
        create:
            - DROP TABLE IF EXISTS vector_delete_768d_indexed;
            - CREATE TABLE vector_delete_768d_indexed (id bigint PRIMARY KEY, embedding vector(768));
            - CREATE INDEX ON vector_delete_768d_indexed USING hnsw (embedding vector_l2_ops) WITH (m=16, ef_construction=200);
        cleanup:
            - DROP TABLE IF EXISTS vector_delete_768d_indexed;
        loadRules:
            - table: vector_delete_768d_indexed
              rows: 200000
              columns:
                  - name: id
                    util: PrimaryIntGen
                    params: [1, 200000]
                  - name: embedding
                    util: DeterministicVectorGen
                    params: [768, -1.0, 1.0, 42]

        executeRules:
            - workload: delete_768d_single_indexed
              customTags: dim=768,operation=delete,deletetype=single,indexed=true
              run:
                  - name: delete_single_row
                    weight: 100
                    queries:
                        - query: DELETE FROM vector_delete_768d_indexed WHERE id = ?
                          count: 10
                          bindings:
                              - util: RandomNumber
                                params: [1, 200000]
