# Vector Update - 768D, update embedding with HNSW index (L2 distance)
# Following VectorDBBench naming conventions
# Configuration: dimension=768, operation=update, column=embedding, indexed=true, m=16, ef_construction=200, distance=l2

type: POSTGRES
driver: org.postgresql.Driver
url: jdbc:postgresql://{{endpoint}}:5433/yugabyte?sslmode=require&ApplicationName=featurebench&reWriteBatchedInserts=true
username: {{username}}
password: {{password}}
batchsize: 128
isolation: TRANSACTION_REPEATABLE_READ
loaderthreads: 4
terminals: 1
collect_pg_stat_statements: true
use_dist_in_explain: true
yaml_version: v1.0
works:
    work:
        time_secs: 120
        active_terminals: 1
        rate: unlimited
        warmup: 30
microbenchmark:
    class: com.oltpbenchmark.benchmarks.featurebench.customworkload.YBDefaultMicroBenchmark
    properties:
        setAutoCommit: false
        create:
            - CREATE EXTENSION IF NOT EXISTS vector;
            - DROP TABLE IF EXISTS vector_update_768d_indexed;
            - CREATE TABLE vector_update_768d_indexed (id bigint PRIMARY KEY, embedding vector(768));
            - CREATE INDEX ON vector_update_768d_indexed USING hnsw (embedding vector_l2_ops) WITH (m=16, ef_construction=200);
        cleanup:
            - DROP TABLE IF EXISTS vector_update_768d_indexed;
        loadRules:
            - table: vector_update_768d_indexed
              rows: 100000
              columns:
                  - name: id
                    util: PrimaryIntGen
                    params: [1, 100000]
                  - name: embedding
                    util: DeterministicVectorGen
                    params: [768, -1.0, 1.0, 42]

        executeRules:
            - workload: update_768d_embedding_indexed_m16_ef200
              customTags: dim=768,operation=update,column=embedding,indexed=true,m=16,ef_construction=200,distance=l2
              run:
                  - name: update_vector_with_index
                    weight: 100
                    queries:
                        - query: UPDATE vector_update_768d_indexed SET embedding = ?::vector(768) WHERE id = ?
                          count: 10
                          bindings:
                              - util: DeterministicVectorGen
                                params: [768, -1.0, 1.0, 43]
                              - util: RandomNumber
                                params: [1, 100000]
