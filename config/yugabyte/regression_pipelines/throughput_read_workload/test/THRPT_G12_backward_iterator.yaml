type: YUGABYTE
driver: com.yugabyte.Driver
url: jdbc:yugabytedb://{{endpoint}}:5433/yugabyte?sslmode=require&ApplicationName=featurebench&reWriteBatchedInserts=true&load-balance=true
username: {{ username }}
password: {{ password }}
batchsize: 128
isolation: TRANSACTION_REPEATABLE_READ
loaderthreads: 1
terminals: 1
collect_pg_stat_statements: true
use_dist_in_explain: true
yaml_version: v1.0
works:
    work:
        time_secs: 180
        active_terminals: 1
        rate: unlimited
        warmup: 60
microbenchmark:
    class: com.oltpbenchmark.benchmarks.featurebench.customworkload.YBDefaultMicroBenchmark
    properties:
        setAutoCommit: true
        create:
            - DROP TABLE IF EXISTS THRPT_G12_employees_idx_backward_scan;
            - CREATE TABLE THRPT_G12_employees_idx_backward_scan (emp_id INT, name VARCHAR(10), email VARCHAR(20), salary INT, hire_date DATE, department VARCHAR(20), is_active BOOLEAN, last_login TIMESTAMP, primary key(emp_id asc)) SPLIT AT VALUES ((333333), (666666));
            - create index login_actvity on THRPT_G12_employees_idx_backward_scan(last_login asc, department, is_active) include(emp_id,name);
        cleanup:
            - DROP TABLE IF EXISTS THRPT_G12_employees_idx_backward_scan;

        loadRules:
            - table: THRPT_G12_employees_idx_backward_scan
              rows: 1000000
              columns:
                  - name: emp_id
                    util: RandomUniqueIntGen
                    params: [1, 1000000]
                  - name: name
                    util: RandomAString
                    params: [10, 10]
                  - name: email
                    util: RandomAString
                    params: [20, 20]
                  - name: salary
                    util: RandomNoWithDecimalPoints
                    params: [1, 100000, 2]
                  - name: hire_date
                    util: RandomDateBtwYears
                    params: [2000, 2025]
                  - name: department
                    util: OneStringFromArray
                    params: [['Engineering', 'Sales', 'Marketing', 'Finance', 'IT', 'HR', 'Legal', 'Product']]
                  - name: is_active
                    util: RandomBoolean
                  - name: last_login
                    util: RandomTimestamp
                    params: [10000000]

        executeRules:
            - workload: THRPT_G12_idx_backward_scan
              run:
                  - name: THRPT_G12_idx_backward_scan
                    weight: 100
                    queries:
                        - query: SELECT emp_id, name, department, hire_date, last_login FROM THRPT_G12_employees_idx_backward_scan WHERE department IN (?,?) AND is_active = ? AND last_login < ? ORDER BY last_login DESC limit 50;
                          bindings:
                              - util: OneStringFromArray
                                count: 2
                                params: [['Engineering', 'Sales','Marketing','Finance','IT','HR','Legal','Product']]
                              - util: RandomBoolean
                              - util: RandomTimestamp
                                params: [10000000]